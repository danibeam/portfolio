// import { selectRepositoriesDomain } from '../selectors';

describe('selectRepositoriesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
